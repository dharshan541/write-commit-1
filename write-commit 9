#include<stdio.h>
int main()
{
  int  i,n,sum=0;
  float num;

  printf(“Enter n:”);
  scanf(“%d”,&n);
  for(i=0;i<n;i++)
   {
      printf(“Enter a number”);
      scanf(“%f”,&num);
      sum=sum+num;
   }
   printf(“sum=%d”,sum);
   return 0;
}
